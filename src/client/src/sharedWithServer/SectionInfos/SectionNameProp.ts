import { SectionName } from "../sectionVarbsConfig/SectionName";

export interface SectionNameProp<SN extends SectionName> {
  sectionName: SN;
}

export interface SnProp<SN extends SectionName> {
  sectionName: SN;
}
