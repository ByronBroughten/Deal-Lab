import { sxProps } from "../../modules/utils/mui";
import { unitSizes } from "./unitSizes";

export const inputStyles = {
  inputLabel: sxProps({
    fontSize: unitSizes.fs18,
  }),
  inputEditor: sxProps({
    fontSize: unitSizes.fs18,
  }),
};
