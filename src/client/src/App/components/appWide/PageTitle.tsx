import { styled } from "@mui/material";
import { nativeTheme } from "../../theme/nativeTheme";
import { SectionTitle } from "./SectionTitle";

export const PageTitle = styled(SectionTitle)({
  fontSize: nativeTheme.pageTitleFs,
});
