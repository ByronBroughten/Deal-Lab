import { styled } from "@mui/material";
import { nativeTheme } from "../../theme/nativeTheme";
import { SectionTitle } from "./SectionTitle";

export const SectionTitleMain = styled(SectionTitle)({
  fontSize: nativeTheme.fs24,
});
