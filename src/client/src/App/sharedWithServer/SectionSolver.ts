export function MakeSectionSolver() {
  return class SectionSolver {
    // *Takes FeSections as an argument
    // *Creates a
  };
}

// I'll put all of them on this one class because it's basically just going to take the section builder
// and solve after everything, right?

//
